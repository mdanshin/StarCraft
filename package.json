{
  "name": "koprulu-frontline",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A Russian-language StarCraft II fan card strategy with offline desktop play and a computer opponent.",
  "scripts": {"start": "node scripts/serve.mjs", "build:desktop": "node scripts/build-desktop.mjs", "check:desktop": "node scripts/build-desktop.mjs --check", "test": "node --test tests/*.test.mjs", "test:browser": "node tests/browser-smoke.mjs"},
  "engines": {"node": ">=20"}
}
